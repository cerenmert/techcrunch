package TechCrunch.Pages;

import TechCrunch.WebDriverHelper;
import org.openqa.selenium.WebDriver;

public class BasePage extends WebDriverHelper {
    public BasePage(WebDriver webDriver) {
        super(webDriver);
    }
}
